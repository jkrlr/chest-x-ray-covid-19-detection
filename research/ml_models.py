# Add the necessary library imports here


# img_url (path of image) is given
# apply preprocessing(resizing image, converting to array, prediction by ml model etc.) on image 
# return probability of covid
def chest_xray_predict(img_url):
    probability = 0.5   # replace it with ml_model prediction probability
    
    return probability